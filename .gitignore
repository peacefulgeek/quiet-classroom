node_modules/
.env
.env.*
*.log
.DS_Store
content/heroes-tmp/
*.zip
*.tar.gz
.cache/

# Article JSON now lives on Bunny CDN (articles/{slug}.json + articles/index.json).
# Kept locally only for one-shot migration / dev mirror. Never commit.
content/articles/
content/heroes-png/
content/brand/
