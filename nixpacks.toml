providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_22", "pnpm-9_x"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile=false --prod=false"]

[phases.build]
cmds = ["echo 'no build step, SSR is runtime'"]

[start]
cmd = "node src/server/index.mjs"

[variables]
NODE_ENV = "production"
